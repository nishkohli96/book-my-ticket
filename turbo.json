{
  "$schema": "https://turborepo.dev/schema.json",
  "tasks": {
    "lint": {
      "cache": false
    },
    "lib:build": {
      "dependsOn": ["^lib:build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build", "^lib:build"],
      "outputs": ["build/**", "dist/**", ".next/**", "!.next/cache/**"]
    }
  }
}
